services:
  frontend:
    build:
      context: ./client
    ports:
      - "3000:3000"
    networks:
      - app-network

  backend:
    build:
      context: ./server
    ports:
      - "8080:8080"
    networks:
      - app-network
    depends_on:
      - frontend

networks:
  app-network:
